# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EUM' do
pod ‘Alamofire’
pod ‘SwiftyJSON’
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EUM

  target 'EUMTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EUMUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
